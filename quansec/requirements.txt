fastapi==0.115.0
uvicorn[standard]==0.30.6
asyncpg==0.29.0
psycopg2-binary==2.9.9
redis==5.0.8
pydantic==2.8.2
pydantic[email]==2.8.2
python-jose[cryptography]==3.3.0
# ── Password hashing ─────────────────────────────────────────────────────────
# Argon2id is the default for new and re-hashed passwords (core/auth.py).
# passlib keeps pbkdf2_sha256 and bcrypt in its scheme list so pre-migration
# hashes still verify and are upgraded in place on the owner's next login —
# dropping them would lock every existing account out permanently.
passlib[bcrypt]==1.7.4
argon2-cffi==23.1.0
python-dotenv==1.0.1
vici==6.0.3
httpx==0.27.2
email-validator==2.2.0
python-multipart==0.0.32

# ── TLS module ───────────────────────────────────────────────────────────────
# The TLS transport itself is standard-library `ssl` only. cryptography is used
# to parse certificates for /api/tls/certificate; it already arrives as a
# python-jose dependency, but is pinned here because the TLS module imports it
# directly rather than transitively.
cryptography>=42.0.0

# ── Tests ────────────────────────────────────────────────────────────────────
# Run with: .venv/bin/pytest tests/
pytest==9.1.1
pytest-asyncio==1.4.0
